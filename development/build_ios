paths=( "submodules/ios-store" "soomla-native/projects/cocos2dx-ios-store" )
build=../build/ios

rm -rf $build
mkdir -p $build

for path in "${paths[@]}"
do
  cd $path
  ./build_all
  cd -
  cp -R $path/build/* $build/
done

